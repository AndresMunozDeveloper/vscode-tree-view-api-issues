{
  "name": "vscode-tree-view-api-issues",
  "displayName": "vscode-tree-view-api-issues",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "id": "treeviewissues",
          "name": "Tree View Issues"
        }
      ]
    },
    "commands": [
      {
        "command": "treeviewissues.commandForMultipleTreeItems",
        "title": "Command for multiple tree items",
        "icon": "$(account)",
        "enablement": "treeviewissues.selectedTreeItemCount >= 1"
      },
      {
        "command": "treeviewissues.commandForASingleTreeItem",
        "title": "Command for a single Tree Item",
        "icon": "$(activate-breakpoints)",
        "enablement": "treeviewissues.selectedTreeItemCount == 1"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "treeviewissues.commandForMultipleTreeItems",
          "when": "view === treeviewissues",
          "group": "navigation@1"
        },
        {
          "command": "treeviewissues.commandForASingleTreeItem",
          "when": "view === treeviewissues",
          "group": "navigation@2"
        },
        {
          "command": "treeviewissues.commandForMultipleTreeItems",
          "when": "view === treeviewissues",
          "group": "1@1"
        },
        {
          "command": "treeviewissues.commandForASingleTreeItem",
          "when": "view === treeviewissues",
          "group": "1@2"
        }
      ],
      "view/item/context": [
        {
          "command": "treeviewissues.commandForMultipleTreeItems",
          "when": "view === treeviewissues"
        },
        {
          "command": "treeviewissues.commandForASingleTreeItem",
          "when": "view === treeviewissues"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.103.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
